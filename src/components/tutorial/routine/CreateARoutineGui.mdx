import CodeBlock from '@theme/CodeBlock';
import routine_create_image from '@site/static/img/get-started/test-automation/routine-create.png';
import routine_create_confirm_image from '@site/static/img/get-started/test-automation/routine-create-confirm.png';

## Create a routine

### Create a project

<ul>
    <li>Click <b>{props.type}</b> in the left menu to go to the project list.</li>
    <li>
        Click <b>Create project</b> to create a project.
        <img src={props.project_create_image} style={{ width: 400, display: 'block' }}/>
        <br/>
    </li>
    <li>
        Enter the project name and click <b>Add</b>.
        <img src={props.project_create_confirm_image} style={{ width: 400, display: 'block' }}/>
        <br/>
    </li>
</ul>

### Create a routine

- Click <b>+</b> to create a routine.
<img src={routine_create_image} style={{ width: 300, display: 'block' }}/>
<br/>

- We will set the 6 items below.
<img src={props.routine_empty_image} style={{ width: 600, display: 'block' }}/>
<br/>

### 1. Select Repository

- <b>Select</b> the repository including the test script and <b>click</b>.
<img src={props.routine_select_repository_image} style={{ width: 400, display: 'block' }}/>
<br/>

:::tip

If you have not yet integrated Git, see [Git Integration](/management/organization/git-integration/) for more information on Git Integration.

:::

### 2. Select App

- Click <b>Select app</b> to select the platform for running the test script.
<img src={props.routine_select_app_image} style={{ width: 500, display: 'block' }}/>
<br/>

- Select the app to run the test script.
<img src={props.routine_select_app_confirm_image} style={{ width: 500, display: 'block' }}/>
<br/>

:::tip

Refer to [Managing Apps](/management/organization/app) for more information on managing apps.

:::

### 3. Select Device

- Click <b>Cloud devices</b> and click <b>+</b>.
<img src={props.routine_select_device_image} style={{ width: 300, display: 'block' }}/>
<br/>

- Select the device to run the test script.
<img src={props.routine_select_device_confirm_image} style={{ width: 300, display: 'block' }}/>
<br/>

:::tip

You can choose any model of that platform.

:::

### 4. Select Working directory

- Select <b>Working directory</b>.
<img src={props.routine_select_working_dir_image} style={{ width: 400, display: 'block' }}/>
<br/>

:::tip

For more information on working directories, see [Git Integration](/management/organization/git-integration).

:::

### 5. Select Environment

- Select <b>Environment</b>.
<img src={props.routine_select_environment_image} style={{ width: 400, display: 'block' }}/>
<br/>

### 6. Write Command

- Copy this command and paste it into the <b>command</b> block like the image below.
<CodeBlock language="shell">{`${props.routine_write_command_script}`}</CodeBlock>
<img src={props.routine_write_command_image} style={{ width: 400, display: 'block' }}/>
<br/>

- Finally, make sure that the setting is the same as the image below.
<img src={props.routine_full_image} style={{ width: 600, display: 'block' }}/>

### Save Routine
- Click <b>Create</b> to save the routine.
<img src={routine_create_confirm_image} style={{ width: 600, display: 'block' }}/>
<br/>

:::tip

Refer to [Managing Routines](/management/project/routine) for more information on managing routines.

:::