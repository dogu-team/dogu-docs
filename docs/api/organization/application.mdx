---
sidebar_position: 1
title: Application
locale: en
---

## Upload Application

```
PUT /v1/organizations/:organizationId/applications
```

### Query Parameter

| Attribute   | type   | required | description                                                                                                                                 |
| ----------- | ------ | -------- | ------------------------------------------------------------------------------------------------------------------------------------------- |
| `organizationId` | string | true     | ID of the organization. You can easily find it on the organization settings page. See [the organization setup documentation](/management/organization/settings) |

### Header

| Attribute       | type   | required | description                                                                                      |
| --------------- | ------ | -------- | ------------------------------------------------------------------------------------------------ |
| `Authorization` | string | true     | Authorization: Bearer ${ACCESS_TOKEN}.<br /> For ACCESS_TOKEN, refer to the [ACCESS TOKEN](/api) |

### Body

| Attribute | type   | required | description                              |
| --------- | ------ | -------- | ---------------------------------------- |
| `file`    | binary | true     | File binary to upload (`.apk` or `.ipa`) |

### Example

```
curl -v -X PUT "https://api.dogutech.io/v1/organizations/${organizationId}/applications" \
-H "Authorization: Bearer ${ACCESS_TOKEN}" \
-H "Content-Type: multipart/form-data" \
-F "file=@${FilePath}"
```

### Response Example {#upload-application-response}

```
HTTP/1.1 200 OK
```
